<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- For HmacSignature and RequestResponse. It carries FrameworkReference Microsoft.AspNetCore.App, which
         is also where AddHttpClient, IOptions and ILogger come from — so no package references are needed
         here beyond this one project. -->
    <ProjectReference Include="..\Venly.Backend.Common\Venly.Backend.Common.csproj" />
  </ItemGroup>

</Project>
